#!/usr/bin/env ruby
# frozen_string_literal: true

require 'shellwords'

Dir.entries('.').sort.each do |entry|
  system "hb_convert #{Shellwords.escape(entry)}" if /.*\.(mkv|avi|m4v)/.match(entry)
end
